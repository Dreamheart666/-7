#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <windows.h>
#include<string.h>
#include <math.h>
#include<windows.h>
int main()
{
	//异或的方法，比较好
	int a = 3;
	int b = 5;
	printf("before: a=%d b=%d\n", a, b);
	a = a^b;
	b = a^b;
	a = a^b;
	printf("after: a=%d b=%d\n", a, b);
	return 0;
}
